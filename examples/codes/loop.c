#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *argv[])
{	
	int i, a, b, c, j, vet[5], aux, __jota23__, azinho, bZAO234;

	/* @BranchProb(0.4, 1) */
	if (argc > 1)
	{
		/* LA01 @LoopMax(100) */
		for (i=0; i < 100; i++) 
		{
			/* LB01 @LoopMax(100) */
			for (j=1; j <=100; j++)
			{
				/* @BranchProb(0.97, 0.50, 0.24, 0.12, 1) */
				if ((!(__jota23__ > i) && azinho == bZAO234 )|| c !=j && !a)
				{
					aux = vet[i];
					vet[i] = vet[j];
					vet[j] = aux;
				}
			}
			a =0;
		}
	}
	else 
	{
		puts("Uso incorreto.");
	}
	/* LB03 @LoopProb(0.75, 0.35, 1) */
	while (a!=b && j>c)
	{
		puts("isso eh um teste.");
	}

	return 0;
}
